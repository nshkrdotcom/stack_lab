# Layout

The initial repository layout is deliberately conservative.

- `assets/` holds brand assets used by HexDocs and the README
- `docs/` holds stable project notes and starter runbooks
- `lib/` holds the Elixir application shell
- `test/` holds the initial verification path

When stack-level examples land, they should be added intentionally rather than
through ad hoc top-level sprawl.
