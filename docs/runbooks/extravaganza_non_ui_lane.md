# Extravaganza Non-UI Lane

Run the credential-free internal fixture from the harness package:

```bash
cd /home/home/p/g/n/stack_lab/support/citadel_spine_harness
mix test test/extravaganza_non_ui_lane_test.exs
```

The fixture proves product pack compilation, typed AppKit runtime projection
readback, Extravaganza workpad rendering, local worker identity flow, SSH
execution-surface contract coverage, required failure variant mapping, and live
readiness classification.

This is not live provider acceptance. Full live Linear to GitHub to Linear E2E
remains blocked until the lane can create or discover a disposable GitHub PR and
drive the full provider workflow through typed non-secret inputs, connection
records, credential leases, source admission, workflow state, and durable
receipts. Static provider object selectors do not satisfy this runbook.
