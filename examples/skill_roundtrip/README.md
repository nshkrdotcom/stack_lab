# StackLab Skill Roundtrip

Phase G proof package for `SKILL-001` through `SKILL-010`.

The roundtrip composes Jido Hive skill contracts, the memory-default skill
engine, AppKit skill DTOs, and the external-author conformance helpers. The
proof keeps manifests and projections ref-only and records provider readiness
only after admission and invocation gates pass.
