# StackLab TRINITY Parity Harness

Executable parity harness for the `trinity_coordinator` to `trinity_framework`
migration. The default run covers fast command-surface, fixture, and boundary
rows. CUDA and full stage parity remain explicit opt-in rows for the final
parity audit.
